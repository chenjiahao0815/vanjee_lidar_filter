

[INFO] [1788917977.923309076] [rosbag2_player]: Pausing play.
[INFO] [1788919222.611304319] [rosbag2_player]: Pausing play.
[INFO] [1788920320.414966158] [rosbag2_player]: Pausing play.
[INFO] [1788921022.492806073] [rosbag2_player]: Pausing play.

/home/linux/ros_cpp/vanjee_lidar_filter/scripts/vanjee_noise_motion_20260902_143028

[INFO] [1788922640.428932775] [rosbag2_player]: Pausing play.
t1要删，t2要留

[INFO] [1788998101.921561962] [rosbag2_player]: Pausing play.
t4删少了





1
我拿同一段 bag 跑了数据，找到一个能真正分开两者的信号：回波强度。

为什么现在这套调不出来
当前唯一的判据是「体素里点的竖直跨度」。一张真实的薄平面和一张噪点薄片，在体素尺度上厚度是一样的——都只有几毫米到几厘米。你圈的这片实测厚度 2.4 cm，法向朝上，10 cm 格里局部跨度中位数才 3 mm。它和噪点在这个维度上没有区别，所以 thr_z_min 往下调就漏噪点、往上调就误删它，中间不存在合适的值。这不是参数问题，是判据只有一维。

强度能分开它们
我在这段 bag 里每隔 25 帧取样、把立方体内的点做欧氏聚类，统计每团的强度中位数：

类别	强度中位数
噪点团（含 fr300 那团 3053 点的大噪幕）
4 ～ 8
你圈的这片（fr700，477 点）
59
墙、地面、其他薄平面
25 ～ 254
差距是一个数量级，中间几乎没有重叠。注意 fr300 那团噪点有 3053 点、2.6×1.6×1.0 m，靠点数或厚度都抓不住，靠强度一眼就出来。

方案：判「物体」，不判「格子」
聚类：立方体内的点做欧氏聚类，连接距离随距离缩放（近处约 0.15 m），得到一个个连通物体。
算每团的特征：强度中位数、点数、PCA 厚度与法向、覆盖线数、单线方位角最长连续段。
判定：强度中位数低于门槛（约 10）→ 判为噪点整团删；点数过少的碎片也删；其余一律留，不再看厚度。
体素那套保留做可视化和合并，但删不删由团决定。
按门槛 10 在 54 帧上试跑：删掉立方体内 13.2% 的点，删掉的大团强度全部落在 4～8；同时 41 个「薄水平面」（厚度 <5 cm、法向朝上、点数 ≥100）一个都没被删，包括你圈的那片。

需要注意的地方
强度低也可能是真实的深色物体。所以第 3 步最好不要只看强度：把「强度低」和「薄、或者孤立不连到大结构、或者跨帧不稳定」组合起来，两个条件都满足才删。这样一块黑色但结实的板子不会被误清。跨帧稳定性需要缓存前几帧，是可选项，前两个条件当前帧就能算。












2
直接回答：分层有好处，但好处不在"比 z"，在"比方位角连续性"。

为什么单纯"分层再比 z"没用
一条扫描线是固定俯仰角的一个圆锥。它打在水平面上，扫出来的一整圈 z 几乎不变——真薄片是这样，噪点扁片也是这样，每层内部的 z 跨度都接近 0，分不开。跨层比 z 就又回到现在的厚度规则了，等于绕一圈回原点。

分层真正值钱的东西
按线组织后，每条线是按方位角排好序的序列，你能直接切段（方位角断开或距离突变就断）。这个量把你圈的那片和噪点分开了，而 z 厚度做不到：

目标	点数	段数	平均每段点数	最长段	强度中位	z 厚度
真墙
4480
135
33
97
58
13 cm
你圈的 t5 薄片
509
15
34
119
60
2.4 cm
小噪点团
127
9
14
33
4
2 cm
大片低强度噪
3033
640
4.7
186
5
4.8 cm
t5 和真墙都是每段 33～34 点，噪点是 4.7～14。厚度上 t5（2.4 cm）比噪点还薄，所以现在这套规则必然误删它；连续性上它和墙是一档。

强度是第二个强信号：真结构 58～60，噪点 4～7。

建议的方案
三层，全部在段级判定，不再用体素厚度：

按线切段：每条线按方位角排序，方位角跳变或相邻点距离突变就断开。
段级打分：段内点数、强度中位数、相邻线有没有距离相近的段托着（真物体上下线互相支撑，空中扁片只有孤零零几条线）。
删整段：短 + 低强度 + 无相邻线支撑 → 删。
这套对你这个案例的结果：t5 段长 34、强度 60、5～7 条线互相支撑 → 留；噪点团段长 14、强度 4、没支撑 → 删。

一个要提醒的坑：我在 fr300 发现一块 3033 点、强度 5、跨 25 条线的大片，段却碎成 640 段（一半的段只有 1 个点）。这种"大而碎"的东西按上面规则会被删，但它连续 100 帧稳定存在，也可能是玻璃或铁丝网之类的真实弱反射物。真要动手前，建议你先在 RViz 上确认这块到底是什么。











先纠正一个措辞，这是整个思路成立与否的关键：我不是想用强度去删点，是想用强度去否决"豁免"。这两件事逻辑地位完全不一样。

为什么只能"否决豁免"，不能"直接定罪"
物理上这是个单向关系：

噪点 → 回波弱。pancake 的成因是串扰、多次回波、近场内反射、灰尘水雾，这几种回波的能量本身就被分掉了或者散射截面极小。所以低强度是成因决定的，不是碰巧相关上。
回波弱 → 噪点。不成立。黑色哑光面、湿地面、掠射角扫到的墙、远处的墙，强度都会掉，因为 I 大致正比于 反射率 × cos(入射角) / r²。
所以强度只能当保护的必要条件（不够亮就别想豁免），不能当删除的判据。一个格失去豁免之后还得再过"薄不薄"那一关才会被删。这样错判的代价是不对称的：一面又暗又薄的真墙会被删（这是这条路唯一真实的失效模式），但一面暗而厚的真墙不受任何影响——而"又薄又暗"恰好就是 pancake 的定义。

为什么非得加这个维度（光调 rms 出不来）
这是我这次查数据最想告诉你的一点。被保护的团按 rms 排开来看：

rms 范围
噪点团（17 个，Imed 4~7）
9.2 / 10.0 / 11.2 / 12.0 / 14.2 / 15.0 mm
真结构团（31 个，Imed 54~144）
2.1 ~ 14.9 mm，你要保的 t5 正好 12.0 mm
两个区间完全套在一起。你要保住 12mm 的 t5，就必然连带保住 rms 9~12mm 的噪点。所以 plane_protect_rms_m 这个参数怎么调都分不开，必须补一个和厚度正交的维度进来。

根本原因是：拟合残差和 z 跨度测的是同一件事——厚度。拟合只是把厚度改成沿法向量，解决了斜面被算厚的问题（这个确实修好了），但它没有带来一丝一毫"真假"信息。噪点本来就是薄片，天生就能通过平面拟合。

强度这条路怎么用才稳
直接拿原始 intensity 和固定阈值比是脆的，因为它随距离和入射角变。有两个更稳的做法：

按距离带定阈值，或者干脆用相对量——这团的强度中位除以同距离带全局强度中位。更值得考虑的是强度离散度：真面同材质同距离，强度很均匀，IQR 只有 5~16；噪点回波随机，IQR 40~138。IQR 是团内部的相对量，整团一起被距离衰减时它不变，比中位数的绝对阈值鲁棒得多。

如果你完全不想碰强度，还有个纯几何的替代：沿方位角的连续段长。真面 33~34 点/段，噪点 4.7~14。我实测这个门槛也能把全部 17 个噪点团挡在保护外，对黑色物体天然免疫，但它更激进——真结构团从 28 个掉到 18 个。

实现上有个成本要提前知道：节点现在用 pcl::PointXYZ，强度字段在转换时就被丢了，得像 ring 那样单独读一份出来。

一个必须说的实测结论：强度门槛救不回删除率
我加上「强度中位 ≥ 20」跑了一遍，17 个噪点团全部不再被保护，真结构只掉 3 个——但删除率只从 3.2% 涨到 3.3%。

原因是那 4005 个被保护的噪点里，只有大约 50 个点所在的格是"薄"的。剩下的在你现在 size_xy_bands: (1.0,20.0),(2.0,22.0),(3.0,14.0) 这个设置下，体素被撑到 1.0~1.1m 宽，一个格的 footprint 太大，噪片自身的起伏加上混进来的邻近结构，z 跨度早就超过 thr_z 了，本来就没被判成薄。

对照一下不加保护的裸删除率：当前这档 11.0%，(1,2),(2,6),(3,10) 那档 17.8%，1-2-3 倍那档 25.4%。

所以噪点活下来的第一原因是体素太粗，保护只是第二层。强度这个维度该加，但得配合把 size_xy_bands 收回去，否则加了也看不出效果。

另外团级粒度我也顺手查了：高强度团内部搭便车的低强度薄格只有 18 格 / 230 点，不是主要矛盾；真正被保护住的是 543 格 / 3848 点的"薄+高强度"，那些大概就是 t5 这类真面。所以粒度这块不用动。




先修正上一条：IQR 那条路是错的，数据把它否掉了。

噪点的强度是"一致地低"——全是 4~8，所以它的绝对离散度反而比真面小（噪点 IQR 中位 5，真结构 12），方向是反的。用 IQR 做门槛挡住 0/17 噪点却误挡 6/31 真结构。变异系数 IQR/中位好一些但也只能挡 10/17。

按距离带归一化我也试了，同样更差：相对值门槛 0.7 挡 34/37，提到 1.0 才全挡住但误挡 39/63。原因是参考值被污染了——r=2.2~2.6 这一带里有 19 个噪点团对 18 个真团，噪点自己把带中位拉低了，于是它显得"正常"。

反倒是最朴素的绝对阈值最干净。100 个团的样本上：

强度中位	Imed ≥ 10 的效果
噪点团 37 个
4 ~ 8（中位 4）
全部挡在保护外 37/37
真结构团 63 个
11 ~ 254（中位 87）
零误挡 0/63
间隙落在 8 和 11 之间。所以方案就是绝对强度中位，不归一化。

实现方案
第一步，把 intensity 取出来。 现在点云转换走的是 pcl::PointXYZ，强度字段在那一步就被丢掉了。照 ring 现成的做法，单独抽一个和点索引对齐的 vector<float>，不改点类型，整条管线不用动。

第二步，给保护加一个必要条件。 位置就在现在设 plane_protected = true 的那一处，三个条件全满足才给豁免：团点数 ≥ plane_protect_min_points、团 rms ≤ plane_protect_rms_m、且团强度中位 ≥ plane_protect_intensity（新参数，默认 10）。中位数用 nth_element 求，O(n)，不碰耗时。

第三步是这个方案能成立的关键——失效模式是安全的。 强度只否决豁免，不否决保留。一个又暗又薄的真面失去豁免以后，回落到原来的 z 厚度判据，也就是加平面保护之前的行为。所以这个门槛的最坏情况就是退回基线，不引入任何新的误删。这是我敢用一个绝对阈值、而不去纠结距离补偿的原因：它只可能收回过度的豁免，不可能造成新伤害。

第四步，必须同时把 size_xy_bands 收回去，否则前三步看不出效果。 光加门槛删除率只从 3.2% 走到 3.3%，因为 20/22 倍把体素撑到 1.0~1.1m 宽，噪片在这么大的 footprint 里 z 跨度早就超过 thr_z，根本没被判成"薄"。裸删除率的对照是：当前这档 11.0%，(1,2),(2,6),(3,10) 档 17.8%，1-2-3 倍档 25.4%。所以要把 bands 调回 2~6 倍量级，再和 plane_protect_intensity 一起扫参数。

可观测性上我想把团的点数、rms、强度中位、是否豁免都打进日志，青色框的标签上也加 Imed——8 到 11 这个间隙不宽，你得能在 rviz 里直接看出某个团凭什么被保住。

两件不做的事：团级粒度不动（搭便车的只有 18 格 / 230 点，不是矛盾所在）；plane_protect_rms_m 不再指望它区分噪点，两边区间完全重叠，它以后只负责"是不是一个面"。

要我按这个动手吗，还是先把第四步的参数扫描结果给你看了再改？






纯几何这条路我摸到天花板了，最好的单一特征是扫描线的长段占比：挡住 31/37 噪点，代价是 4/63 真结构失去保护。

判据	挡住噪点	误挡真结构
现在（只有点数 + rms）
0/37
0/63
长段(≥3)点占比 < 0.94
31/37
4/63
强度中位 < 10（对照）
37/37
0/63
为什么长段占比能成立
激光扫过一个真实表面时，相邻方位角的采样必然连续命中——激光不可能从表面中间跳过去。所以真面在每条 ring 上都是一段连续的点。噪点是零星回波，段长中位数是 1，也就是每条线上都是孤立的单点，而真结构是 17。

这是成因层面的差别，跟厚度完全正交，这也是它为什么能补上平面拟合缺的那一维。落地也不贵：程序里 ring_of_cur_ 已经存了从原始消息取的真线号，团里已经有点索引，只要在 planeFitRms 那一处顺手按 ring 分桶、方位角排序、数一下段长就行。只在通过了 rms 的团上做，每帧就几十个团，耗时可以忽略。注意现有的 ring_mask 只是个 64 位"哪些线有点"的掩码，段长得另算。

几条已经死掉的路（省得再试）
跨帧持续性彻底无效。噪点团和上一帧的同格重合率中位 0.95，真结构也是 0.95，一模一样。这说明这个 pancake 不是随机闪烁的噪声，而是相对雷达位置固定的系统性伪影，所以它跟真墙一样稳。这一条否掉了"多帧投票""时序滤波"这一整类方法。

形状类特征全是糊的。λ1/λ2 长宽比噪点 0.51、真面 0.58；法向 |z| 噪点 0.06、真面 0.07；团中心 z、点数、覆盖 ring 数、填充率也都区间重叠，最好的也就是"挡 35 误 23"这种没法用的水平。原因是 pancake 在几何上真的就是个规规矩矩的小平面片，形状上和一小块真墙没区别。

相邻 ring 覆盖率、含长段的 ring 占比这两个我做的细化版都不如原始的长段占比，AND 组合也没能同时做到挡 33 以上、误挡 8 以下。

漏网的 6 个是什么，以及一个还没测的方向
那 6 个逃掉的噪点团，长段占比 ≥ 0.94，也就是它们看起来就是完整的扫描线。这类大概是镜面反射产生的"镜像面"——几何上它是一个货真价实的面，只是位置是假的。纯扫描线结构分不出来。

但对付这类有个我还没测的判据，而且纯几何：射线穿透一致性。真表面会遮挡，沿雷达射线打过去，它后面不该再有回波。镜像/虚像不遮挡，同一条射线穿过它以后还会打到更远的真墙上，于是同一个方位角+ring 上存在比它更远的点。这个可以直接用 ring + 方位角格去查，不用建八叉树，成本也不高。












长段占比不能直接用。 在正确的对象上重测，它彻底失效了：

长段占比
t1 类（水平薄片，你说是噪点）
0.84 ~ 1.00（中位 0.98）
疑似地面/车顶
0.91 ~ 0.99（中位 0.97）
墙
0.29 ~ 1.00（中位 0.93）
三类完全套在一起，而且方向是反的——t1 的长段占比比墙还高。拿它当门槛，挡住 18/37 个 t1 的同时要误挡 84/154 面墙。原来那个「31/37、误挡 4/63」纯粹是因为样本里没有 t1 这类东西。

有比它更好的：法向 + 线数
同一批数据里，这两个量分得很干净：

覆盖线数	z 跨度	法向 |z|
t1 类（37 团）
2 ~ 5（中位 3）
0.02 ~ 0.26（中位 0.05）
≥0.9
疑似地面/车顶（22 团）
6 ~ 10（中位 8）
0.06 ~ 0.23
≥0.9
墙（154 团）
6 ~ 19（中位 9）
0.23 ~ 0.79
<0.2
门槛扫描的结果比长段占比好一个量级：

规则「法向|z|≥0.9 且 线数≤T → 否决豁免」	挡住 t1 类	误挡墙	误挡地面/车顶
T=4
33/37
0/154
0/22
T=5
37/37
0/154
0/22
T=6
37/37
0/154
2/22
T=5 时把 37 个 t1 类全部挡住，墙和多线水平面一个都没误挡。强度那一列顺便也说明了为什么强度靠不住：t1 类中位 110，墙中位才 10——墙比噪点暗得多，我之前的方向整个是反的。

但我不打算就这么写进去
这里有个坑我必须讲明白：上面「t1 类 / 地面车顶 / 墙」这个分类，是我用法向和线数自己切出来的，然后再用法向和线数去验证它。又是自证。

它比强度那次强的地方在于：t1 是你亲手圈的，法向≈1、3 条线、z 跨 4cm 这几个数是从你圈的那团实测出来的，不是我编的。但「线数 6~10 的水平面就是地面或车顶」纯粹是我的猜测，我没有任何证据说那 22 团里没有 pancake，也没证据说里面真有车顶。

所以差的就是一件事：几帧真标注。而这个成本很低，现在的程序已经把青框标了 t1/t2，日志里也在打每团的点数和 rms。

我建议这样：我先只加日志——给每个拿到豁免的团补打 法向|z|、线数、z跨，青框标签上也显示。你暂停几帧，看着 RViz 告诉我哪几个 t 号是噪点、哪几个是车顶。拿到真标注后，线数≤5 这个门槛该定几、要不要加，一次就能定死，不用再来回猜。

要我先加这个日志吗？还是你觉得 T=5 这组数字够说服力，直接按格级把它写进豁免逻辑？





这一帧说明什么
强度在这一帧是反的。 噪点 t1 的 Imed=107，非噪点团只有 6～32。强度≥10 恰恰把唯一的噪点保下来了。

单看法向不行。 t2/t3/t4 和绿点也几乎全是水平的（|nz| 0.93～1.00），和 t1 一样躺着。

线数这一帧能分开。 t1 只有 3 条线（24/25/26）；非噪点团是 5～10；绿点是 10。门槛「|nz|≥0.9 且 线数≤5 → 否决豁免」在这一帧会否决 t1，且不会动到当前任何非噪点豁免（它们本来就没拿到豁免）。

长段占比分不开。 t1 是 0.94，其它团 0.93～0.99。

rms 也分不开真假，只决定谁进豁免池。 t1 极平所以进了池；t2/t4 太厚进不了。问题出在进池之后缺第二票。

格级在这一帧的情况
t1 的 61 个薄格里，格内点数中位只有 3，单格 PCA 不稳：线数中位掉到 1，用同一门槛只能否决约 36/61 格。团级一票否决更干净；格级会漏一部分薄格继续被保。

小结
这一帧支持的方向是：豁免逻辑里加「水平 + 少线」否决票，优先团级。
不支持的是：强度门槛、长段占比、单看法向、射线穿透。

局限也说清楚：这还只是你标的一帧。t3 有 5 条线、也是水平，只是 rms 大没进豁免；如果以后某帧它变薄进了豁免池，线数≤5 也会否决它——需要你再确认那种团算不算噪点。





测完了。标签不用强度，只用你标的那帧 t1 的空间位置在 bag 里追踪（100 个噪点团 vs 302 个竖直墙豁免团）。

结论总表
方法	对 t1 有用吗	证据
扫描线长段占比
不可用
t1 中位 0.96，墙中位 1.00，几乎一样；想挡住 t1 就会大伤墙
段长中位 / 含长段 ring
不可用
区间重叠
邻 ring 覆盖
很弱
t1 略低，分不开
跨帧持续性
不可用
t1 0.85，墙 0.82，一样稳
长宽比 λ1/λ2
不可用
重叠
法向 |nz|
对墙极强
可分性 31；t1≈1.0，墙≈0.02
线数
有用，但不完美
t1 中位 4，墙中位 8；单用会误伤其它水平面
z 跨度
中等
t1 更薄，有重叠
强度
方向反了
t1 更亮（中位 110），墙更暗
硬标注邻帧也对得上：fi=731/735/740 的 t1 都是 lf≈0.94、|nz|=1、线=3、I≈108——长段连续，不是碎点。

对 md 那段话的裁定
以前是判断错了，不是“逻辑框架写错公式”。

“噪点扫描线碎、真面连续”→ 对 t1 说反了，这条方法废
“跨帧无效”→ 仍然对
“形状/法向全糊”→ 测错了；|nz| 分 t1 和墙非常干净
强度当噪点判据→ 对 t1 也反了
还能用的组合
|nz|≥0.9 且 线数≤5：墙误伤 0/302；t1 召回约 61/100（空间盒里混进了一些多线水平团，所以不是 100%）。
代价是其它水平豁免团会被否掉一批（77/163）——里面有没有车顶，还要你再标。

一句话：md 里主推的长段占比，对现在的 t1 没用；真正有信号的是法向+线数。



对着暂停时间戳查日志，看这团为什么没被「水平少线」否决。

原因很清楚，不是规则没跑，是线数门槛没卡住它。

暂停前后那一帧的日志：

平面保护: t2 点数=531 rms=0.0093m Imed=110.0 |nz|=1.00 线=8 → 整团不删
对照否决条件 |nz|≥0.9 且 线数≤5：

条件	这团	结果
|nz|≥0.9
1.00
满足
线数≤5
8
不满足
两个要同时成立才否决。法向已经判成水平薄片，但覆盖了 8 条线，超过 plane_protect_max_rings: 5，所以照样给豁免，86 个格整团留下。

上一帧你标的 t1 只有 3 条线，能被否掉；这一帧同类噪点更大、跨了更多 ring，就漏网了。

可选办法只有两条：

把 plane_protect_max_rings 调到 8 或 9（能吃掉这团，但更容易误伤车顶/地面）
加第二维（比如 z 跨度、距离），不单靠线数
你要先只改 yaml 试 plane_protect_max_rings: 8，还是先别动、再标几帧车顶再定？